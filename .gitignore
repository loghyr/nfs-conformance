# Build artifacts: every op_* binary but not the .c/.h sources.
op_*
!op_*.c
!op_*.h

# Second-client probe binaries (see README charter)
cb_getattr_probe
cb_recall_probe
server_caps_probe

# Compiler artifacts
*.o
*.dSYM/

# Scratch files left behind if a test aborts before cleanup.
t_*
t1[0-5].*
